> "Regular expressions are kinda like functional Brainfuck"
> 							© Some lad on Y-combinator
# Основы

Ну, в целом там не сложно. Вот [краткий референс](https://www.regular-expressions.info/quickstart.html)

Из более сложного могу отметить группы, которые можно создавать `()` и которые можно использовать далее при помощи `\n`, где `n` номер группы

Также классы и множества символов, например `[[:alnum:]]` или `[0-9a-zA-Z]` — буквы и цифры. Второе — это общий синтаксис для множества символов. Для мэтча какой-либо символ из квадратных скобочек должен совпасть `{n}` раз. Также можно инвертировать при помощи `[^]`
# Сложные случаи
## Lookahead и lookbehind

В некоторых случаях нам нужно найти соответствия шаблону, но только те, за которыми или перед которыми следует другой шаблон. Для этого в регулярных выражениях есть специальный синтаксис: опережающая (lookahead) и ретроспективная (lookbehind) проверка

`\d+(?=€)` — ищет совпадение в цифре, но только если после неё идёт знак `€` 

`(?<=\$)\d+` — тот же вариант, но для цифр, до которых стоит `$`

`\d+(?=\s)(?=.*30)` — такие проверки можно цеплять друг за другом. Тогда совпадут они ТОЛЬКО если обе проверки найдут совпадение

Сами символы в проверке не включены в совпадение — это просто проверка. Но если они тоже нужны в группе захвата, можно обернуть их в дополнительные скобки

`\d+(?=(€|kr))` — 1 и более цифр, за которыми стоит либо `€`, либо `kr`, захватываемые в отдельную группу

Аналогичный синтаксис для lookbehind — `(?<=(\$|£))\d+`
## Рекурсия

Рекурсия в регулярках помогает преимущественно со вложенными и повторяющимися паттернами — такими как палиндромы и вложенные скобочки

`\w{3}\d{3}(?R)?` — такой паттерн по-простому означает "подставь `\w{3}\d{3}` на место `(?R)` и пусть там будет 1 или 0 совпадений". Такое выражение движок будет углублять до тех пор, пока на определённой глубине совпадение не выдаст пустую строку — тогда выражение начнёт схлопываться обратно на глубины вверх и выдавать их совпадения

Пример для строки "aaa111bbb222"
```
**#    Depth   Position in Regex   String Pos.     Notes                     Backtracks**  
S1   D0      **\w{3}**\d{3}(?R)?     **aaa**111bbb222    Match.  
S2   D0      \w{3}**\d{3}**(?R)?     aaa**111**bbb222    Match.  
S3   D0      \w{3}\d{3}**(?R)?**     aaa111**bbb**222    Try Depth 1.   
S4   D1      **\w{3}**\d{3}(?R)?     aaa111**bbb**222    Match.                    3  
S5   D1      \w{3}**\d{3}**(?R)?     aaa111bbb**222**    Match.                    3  
S6   D1      \w{3}\d{3}**(?R)?**     aaa111bbb222**#**   Try Depth 2.              3  
S7   D2      **\w{3}**\d{3}(?R)?     aaa111bbb222**#**   No Match. Back to S6.     6,3  
S8   D1      \w{3}\d{3}(?R)?**#**    aaa111bbb222**#**   D1 succeeds, back to D0   3  
S9   D0      \w{3}\d{3}(?R)?**#**    aaa111bbb222**#**   D0 succeeds.  
  
**Return overall match: aaa111bbb222**
```

Такие рекурсивные глубины атомарны — поэтому для такого паттерна `(\w)(?:(?R)|\w?)\1` работает поиск палиндромов — мы ищем символ слова `(\w)`, в конце повторяем эту группу `\1`, а посередине мы делаем незахватываемую группу через `(?:` и эта группа совпадает с рекурсивным выражением `(?R)` или 0 или 1 буквой `\w?` 

```
dontmatchme -----> sorry, no match  
kook ------------> kook  
book ------------> oo  
paper -----------> pap  
kayak -----------> kayak  
okonoko ---------> okonoko  
aaaaa -----------> aaaaa  
bbbb ------------> bbb
```